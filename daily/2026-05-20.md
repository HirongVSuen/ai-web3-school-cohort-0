# 每日学习笔记 - 2026-05-20

> Rust + Rig + Arbitrum Solo 路线 · Phase 0：环境搭建

## 今日课程

- 已确认 WCB Learning 面板需要登录，暂无公开课程安排
- 按个人学习计划（Rust/Rig/Arbitrum）走 Phase 0 环境搭建

## 本日计划

### Phase 0：环境搭建（预计 1-2 小时）

#### 1️⃣ Rust 工具链检查

```bash
rustup update
rustc --version
cargo --version
```

#### 2️⃣ 安装 Foundry

```bash
curl -L https://foundry.paradigm.xyz | bash
foundryup
```

验证：`forge --version && cast --version`

#### 3️⃣ 创建 Rig 项目

```bash
cargo init rig-agent
cd rig-agent
cargo add rig rig-onchain-kit alloy
```

#### 4️⃣ 配置 Arbitrum Sepolia RPC

- 注册 Alchemy / Infura 获取 Arbitrum Sepolia RPC URL
- 设置环境变量：`ARB_SEPOLIA_RPC`、`PRIVATE_KEY`

#### 5️⃣ 创建测试钱包 + 领水

```bash
# 用 cast 生成新钱包
cast wallet new

# 导出私钥（写在 .env 里）
```

- 到 https://faucet.quicknode.com/arbitrum/sepolia 领测试 ETH
- 验证：`cast balance <地址> --rpc-url <RPC_URL>`

#### 6️⃣ 写第一个 Rig + 链上交互

写一个简单的 Rig Agent，调用自定义 Tool 查询地址余额。

```
rig-agent/
└── src/
    └── main.rs       # Agent 入口：查余额 Tool
```

## 学习记录

### 核心概念

- **Rig 基础模式**：Provider（模型提供商）→ Agent（含 Preamble）→ Tool（自定义能力）→ Prompt（用户交互）
- **rig-onchain-kit**：EVM 交互的 Rust 抽象层，封装了 alloy 的 RPC 和签名
- **Arbitrum Sepolia**：EVM 兼容 L2 测试网，零成本实验环境

### 动手实践

- [ ] Rust 工具链就绪
- [ ] Foundry 安装成功
- [ ] Rig 项目可编译
- [ ] Arbitrum Sepolia 测试网可连通
- [ ] 钱包创建 + 测试 ETH 到账
- [ ] Rig Agent + 查余额 Tool 跑通

### 遇到的问题

-

## 打卡草稿

```
今日学习内容：搭建 Rust + Rig + Arbitrum 开发环境，创建 Rig 项目并配置 Arbitrum Sepolia 测试网络。
学习时长：1-2 小时
心得 / 收获：Rig 的 Tool trait 设计非常 Rustacean，#[async_trait] + 泛型 Provider 的组合让 Agent 扩展很自然。配合 rig-onchain-kit + alloy，EVM 交互链路的类型安全比 JS 方案强很多。
```

## 打卡状态

- [ ] 已打卡
- [ ] 打卡链接：
